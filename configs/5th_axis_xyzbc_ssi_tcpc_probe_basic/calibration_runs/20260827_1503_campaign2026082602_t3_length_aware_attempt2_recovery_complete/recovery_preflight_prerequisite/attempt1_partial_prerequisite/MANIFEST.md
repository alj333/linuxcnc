# Archive Manifest

This directory preserves the retired T3 length-aware Attempt 1 exactly after
its sequence-23 quality abort and the subsequent controller shutdown.

`PARTIAL_REPORT.md` records the failure classification and recovery boundary.
`SHA256SUMS` seals every other regular file in this directory. Verify from
this directory with `sha256sum -c SHA256SUMS`.

The archive is evidence only. Do not run its G-code or append its CSV files.
